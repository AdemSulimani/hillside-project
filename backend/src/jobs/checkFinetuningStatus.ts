/**
 * Poll fine-tuning job status, persist model ids, and handle completion or failure.
 * activate when switched to GPT-4o
 */
export async function checkFinetuningStatus(): Promise<void> {
  // activate when switched to GPT-4o
}
