/**
 * Display target for the feedback collection progress card.
 * Matches the default `FINETUNING_THRESHOLD` on the server unless you change both.
 */
export const FEEDBACK_FINETUNING_COLLECTION_TARGET = 50;
